"use client";
import {useState} from "react";
import {createClient} from "@/lib/supabase/client";
export default function Recover(){const [email,setEmail]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState("");return <main className="max-w-md mx-auto p-8 space-y-6"><h1 className="text-2xl font-bold">Recuperar senha</h1><form className="space-y-4" onSubmit={async e=>{e.preventDefault();setBusy(true);try{await createClient().auth.resetPasswordForEmail(email,{redirectTo:window.location.origin+"/api/auth/callback?next=/redefinir-senha"});setMessage("Se o endereço estiver cadastrado, você receberá as instruções. Verifique também o spam.");}catch{setMessage("Não foi possível solicitar agora. Tente novamente.");}finally{setBusy(false);}}}><label>E-mail<input name="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="block border rounded p-3 w-full"/></label><button disabled={busy} className="bg-blue-700 text-white p-3 rounded">{busy?"Enviando…":"Enviar instruções"}</button></form><p role="status">{message}</p><a href="/login" className="underline">Voltar ao login</a></main>;}
