"use client";
import {useState} from "react";
import {createClient} from "@/lib/supabase/client";
export default function RegisterCollaborator(){const [busy,setBusy]=useState(false),[message,setMessage]=useState("");return <main className="max-w-md mx-auto p-8 space-y-6"><h1 className="text-2xl font-bold">Conta de colaborador</h1><p>Após confirmar o e-mail, peça ao proprietário da oficina para vincular sua conta na área Equipe.</p><form className="space-y-4" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);try{const {error}=await createClient().auth.signUp({email:String(f.get("email")),password:String(f.get("password")),options:{data:{full_name:String(f.get("name")),account_type:"workshop"},emailRedirectTo:window.location.origin+"/api/auth/callback?next=/login"}});if(error)throw error;setMessage("Cadastro solicitado. Confira seu e-mail e solicite o vínculo à oficina. Se já tem conta, use a recuperação de senha.");}catch{setMessage("Cadastro não concluído. Confira os dados ou recupere sua senha se já possui conta.");}finally{setBusy(false);}}}><label>Nome completo<input name="name" minLength={3} maxLength={160} required className="block w-full border p-3"/></label><label>E-mail<input name="email" type="email" required className="block w-full border p-3"/></label><label>Senha<input name="password" type="password" autoComplete="new-password" minLength={10} required className="block w-full border p-3"/></label><a href="/legal" className="block underline">Privacidade e termos</a><button className="bg-blue-700 text-white rounded p-3" disabled={busy}>Criar conta</button></form><p role="status">{message}</p><a href="/login" className="underline">Entrar</a></main>;}
